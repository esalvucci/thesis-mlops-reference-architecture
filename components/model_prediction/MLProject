name: stock_forecast

conda_env: src/utility/config/mlflow_env.yaml
# Can have a docker_env instead of a conda_env, e.g.
# docker_env:
#    image:  mlflow-docker-example

entry_points:
  main:
    parameters:
      dataset_path: path
      output_path: str
    command: "python3 src/model_prediction.py {dataset_path} {output_path}"

#  validate:
#    parameters:
#      data_file: path
#    command: "python validate.py {data_file}"